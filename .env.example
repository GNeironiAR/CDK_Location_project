# Frontend Environment Variables
VITE_APP_API_URL=https://your-api-gateway-url.execute-api.region.amazonaws.com/prod

# AWS Configuration (if needed locally)
AWS_REGION=us-east-1
AWS_PROFILE=[your-profile]

